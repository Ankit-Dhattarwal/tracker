
  /*

  ------>  Map<String, dynamic> toJson() --------

   This method convert our data to json why bcz we want to store data
   locally so some strucutre fomart need too store the data then we use json format
   that are popularly used that why we create function to convert the data to json

   */


  /*

  -------->  factory Transaction.fromJson(Map<String, dynamic> json) -----------

  Then ankit dhattarwal create this method to convert the json to object ( Transaction class
  object ) , then ankit dhattarwal say , to show the data to user that's why we convert the
  json data to object , then think can we direct show data from json why create object
  then simple that deal with object is easy like you can direct acccest the data by dot opertaion
  and that's why ankit create this method to convert the json data to objet.

   */